#!/bin/bash
# WayOfMono wouser Launcher
# Auto-generated by --init

# Ensure we use the local .wo directory
export WOUSER_CODING_AGENT_DIR="$PWD/.wo"

# Run the local binary from node_modules
if [ -f "./node_modules/.bin/wouser" ]; then
  exec "./node_modules/.bin/wouser" "$@"
else
  exec pnpm exec wouser "$@"
fi
