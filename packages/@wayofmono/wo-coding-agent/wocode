#!/bin/bash
# WayOfMono wocode Launcher
# Auto-generated by --init

# Ensure we use the local .wo directory
export WOCODE_CODING_AGENT_DIR="$PWD/.wo"

# Run the local binary from node_modules
if [ -f "./node_modules/.bin/wocode" ]; then
  exec "./node_modules/.bin/wocode" "$@"
else
  exec pnpm exec wocode "$@"
fi
