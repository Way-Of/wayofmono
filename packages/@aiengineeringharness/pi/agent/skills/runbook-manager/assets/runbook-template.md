<!-- AUTO: This section is regenerated by /update_runbook. Manual edits above the marker are preserved. -->
# <project> Production Run Book

**Version:** 1.0 | **Last Updated:** <date> | **Environment:** Production (<url>)

---

## Quick Reference

<!-- AUTO: auto-filled from infra scan -->

| Component | URL / Command |
|-----------|---------------|
| Production App | <production-url> |
| Local Dev | <local-dev-url> |
| Container Logs | `podman logs <container> --tail 100` |
| Config | <config-path> |
| Secrets | <secrets-path> |

---

## Common Issues & Fixes

<!-- MANUAL: add entries via /update_runbook --add-issue -->

### 1. <Symptom Title>
**Symptom:** <description>
**Cause:** <root cause>
**Fix:**
```bash
<fix commands>
```

---

## Deploy Procedure

<!-- AUTO: auto-filled from Dockerfile + justfile -->

### Local Development
```bash
<local dev command>
```

### Production Deploy
```bash
<build commands>
<restart commands>
<verify commands>
```

---

## Config Files Reference

<!-- AUTO: auto-filled from relevant config files -->

### `infra/Caddyfile`
```
<caddy config>
```

### `config/prod_runtime.exs`
```
<runtime config>
```

---

## Verification Checklist (Post-Deploy)

<!-- AUTO: auto-filled from Caddyfile routes + quadlet ports -->

- [ ] `<url>/` → 200
- [ ] `<url>/login` → 200
- [ ] `<url>/health` → 200
- [ ] `podman logs <app>` — No errors

---

## Rollback Procedure

```bash
podman images localhost/<app>
podman tag localhost/<app>:<previous-tag> localhost/<app>:latest
systemctl --user restart <app>-app.service
```

---

## Emergency Contacts

<!-- MANUAL: add via /update_runbook --add-contact -->

| Name | Role | Contact |
|------|------|---------|
| <name> | <role> | <contact> |

---

*Run Book v1.0 | Update after each production incident*
