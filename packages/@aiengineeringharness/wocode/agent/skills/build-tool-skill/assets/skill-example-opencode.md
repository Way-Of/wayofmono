---
name: example-skill
description: "Example skill for OpenCode — uses kebab-case dir naming and lowercase allowed-tools"
version: "1.0"
allowed-tools: read, write, bash, grep, glob
---

# Example Skill for OpenCode

This file demonstrates the correct frontmatter for an OpenCode skill.

## Requirements
- Directory name: `example-skill` (kebab-case)
- `name` field matches directory name exactly
- `allowed-tools` values are lowercase
- Config directory: `~/.config/opencode/skills/`
