name: init_harness
description: "Initialize the AI Engineering Harness in a repository by creating the `thoughts/` directory structure and cloning the f-rr-d repository."
version: 2.0.0
tools:
  - read_file
  - write_file
  - run_shell_command
