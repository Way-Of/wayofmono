# Design Template — auto-generated by Step 1 codebase investigation
# All values must be detected from the project — never hardcode defaults
# Empty fields will use the base theme's professional fallbacks

brand_colors:
  primary: ""          # Main brand color (headings, primary elements)
  primary_light: ""    # Lighter variant
  primary_dark: ""     # Darker variant
  secondary: ""        # Accent/secondary color
  secondary_light: ""  # Lighter accent
  accent: ""           # Highlight / CTA color
  success: ""          # Positive metrics (if brand-specific)
  warning: ""          # Warnings
  danger: ""           # Risks / negative
  info: ""             # Information callouts
  bg: ""               # Page background
  bg_alt: ""           # Alternate background
  text: ""             # Body text
  text_light: ""       # Muted text
