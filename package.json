{
  "name": "wayofmono",
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r --parallel typecheck",
    "publish": "pnpm -r publish --access public --no-git-checks",
    "version": "changeset version",
    "publish:versioned": "pnpm changeset publish --no-git-checks",
    "dev:dashboard": "bash scripts/dev-dashboard.sh"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "@wayofmono/wo-coding-agent": "^1.0.4",
    "wo-coding-agent": "link:../../packages/@wayofmono/wo-coding-agent"
  },
  "devDependencies": {
    "@changesets/changelog-github": "^0.5.2",
    "@changesets/cli": "^2.31.0",
    "@eslint/js": "^10.0.1",
    "eslint": "^10.4.1",
    "prettier": "^3.8.4",
    "typescript-eslint": "^8.61.0"
  }
}
